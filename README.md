# da-crawler-implementation
----
# Task
Используя данные из dataset, необходимо провести аналитику, а также дополнительно реализовать логику локального краулера для итерации и поиска документов по данным внутри (по аналогии с текстовым поиском с доступом к нескольким файлам).
----
# Run
создать .venv, запустить через source venv/bin/activate потом python3 название файла)